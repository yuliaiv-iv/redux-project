import classes from './UserProfile.module.css';

const UserProfile = () => {
  return (
    <main className={classes.profile}>
      <h2>User Profile</h2>
    </main>
  );
};

export default UserProfile;
